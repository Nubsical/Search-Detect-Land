# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# IDE
.idea/
.vscode/
*.swp

.claude/

# OS
.DS_Store
Thumbs.db

# Calibration capture frames (big; the resulting .pkl files ARE committed)
resources/calibration/captures/

# Build artifacts
*.o
*.out
build/

# Secrets
*.env

# Lock files (debatable; keeping uv.lock is fine, removing if you want)

# Flight logs (FlightLog.py) -- one .log + .csv per run, plus the annotated
# camera recording (FlightVideo.py, .mp4). Never committed.
logs/

# Windows alternate-data-stream junk (appears via WSL)
*:Zone.Identifier
